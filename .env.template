# Backend
BACKEND_PORT=5000
APP_ENV=development
IP_SALT=peper # openssl rand -base64 32
DB_PATH=data/bulb.db
ALLOWED_HOSTS=my-site.com,not-my-site.com
RETENTION_LIMIT= # keep empty to keep all toggles

# Frontend
FRONTEND_PORT=5001
VITE_API_BASE=http://localhost:${BACKEND_PORT}
VITE_WS_BASE=ws://localhost:${BACKEND_PORT}